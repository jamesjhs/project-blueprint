# <<PROJECT_TITLE>>

<<PROJECT_DESCRIPTION>>

## Scripts

- `npm run install:all` — install root and package dependencies
- `npm run build` — build the project
- `npm run start` — start the production server
- `npm run dev` / `npm run dev:server` / `npm run dev:client` — development workflows depending on project type

## Environment

Copy `.env.example` to `.env` and update the values for your deployment.

## Generated from project-blueprint

This project was scaffolded from the James JHS `project-blueprint` template system in <<YEAR>>.
